# Nexor Project Branch

Codigo real do produto.

```text
project/
  frontend/
    web/       React SPA institucional Nexor + Biteplaner
    ...        futuros frontends, como admin, landing dedicada ou app mobile/web
  backend/
    api/       Backend Node.js + TypeScript + Fastify
    ...        futuros backends, workers, jobs, webhooks ou BFFs
  packages/
    shared/    Tipos/utilitarios compartilhados futuros
    ui/        UI package futuro, se necessario
```

Contexto para agents fica em `.ai/`.
